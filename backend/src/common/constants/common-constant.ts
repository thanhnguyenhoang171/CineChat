export enum RoleLevel {
  ADMIN = 0,
  MANAGER = 1,
  USER = 2,
}

export enum ActiveStatus {
  ACTIVE = 1,
  INACTIVE = 0,
  PENDING = 2,
}

export enum LoginProvider {
  GOOGLE = 0,
  USERNAME = 1,
}
